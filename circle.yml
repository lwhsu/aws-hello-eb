machine:
  python:
    version: 2.7.11
  services:
    - docker

dependencies:
  pre:
    - pip install --upgrade pip
    - pip install awsebcli

test:
  override:
    - true

deployment:
  prod:
    branch: master
    commands:
      - ./deploy-app.sh
